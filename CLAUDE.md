# Claude Code — Derabona

@AGENTS.md

The imported `AGENTS.md` is the shared project guide and source of truth for repository workflow, architecture, gameplay invariants, verification and publishing. Read it before making changes, including its fresh-upstream/worktree checks and separate Pages/Supabase release boundaries. Keep common instructions there rather than duplicating them here.

Read `README.md`, `DESIGN.md`, `TESTING.md` and relevant `docs/adr/` decisions for the task. Preserve already-pushed changes, the single-file offline guest artifact and existing saved games. Check current implementation and later migrations rather than treating historical verification notes as current state. Report actual verification results, including known failures or diagnostic exclusions.
